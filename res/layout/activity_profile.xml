<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ProfileActivity" >

    <EditText
        android:id="@+id/etParamName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/ibParamIcon"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@+id/ibParamIcon"
        android:ems="10" />

    <ImageButton
        android:id="@+id/ibParamIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:background="#80000000"
        android:paddingRight="10dp"
        android:src="@drawable/new_profile_icon" />

    <Button
        android:id="@+id/bnProfileSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/etParamName"
        android:text="Сохранить" />

    <Button
        android:id="@+id/bnProfileCancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/bnProfileSave"
        android:layout_alignLeft="@+id/ibParamIcon"
        android:text="Отмена" />

    <Button
        android:id="@+id/bnProfileDelete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/bnProfileSave"
        android:layout_alignBottom="@+id/bnProfileSave"
        android:layout_toLeftOf="@+id/bnProfileSave"
        android:text="Удалить" />

</RelativeLayout>